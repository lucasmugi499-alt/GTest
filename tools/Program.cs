using Cascade.Sim;

// Headless runner. M4 adds the text-playable mode (`play`), M6 the batch mode (`batch`).
Console.WriteLine($"CASCADE headless runner · {SimInfo.Slice} · {SimInfo.Milestone}");
Console.WriteLine("Nothing to simulate yet. The sim core arrives in M1.");
